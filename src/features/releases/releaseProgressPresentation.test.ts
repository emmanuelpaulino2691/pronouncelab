import{describe,expect,it}from"vitest";import{releaseLessonPresentation}from"./releaseProgressPresentation";
describe("Release Course progression presentation",()=>{it("visibly distinguishes available, completed, and locked Lessons",()=>{expect(releaseLessonPresentation("available")).toMatchObject({label:"Available",linked:true});expect(releaseLessonPresentation("completed")).toMatchObject({label:"Completed",linked:true});expect(releaseLessonPresentation("locked")).toMatchObject({label:"Locked",linked:false});expect(new Set([releaseLessonPresentation("available").className,releaseLessonPresentation("completed").className,releaseLessonPresentation("locked").className]).size).toBe(3)})});
