import{describe,expect,it}from"vitest";import{completedActivityIdsForSession,initialReleaseSessionMode,releaseLessonRuntimeKey,shouldUseFinalReviewActions,shouldWriteReleaseActivity}from"./releaseLessonSession";
describe("completed Release Lesson sessions",()=>{it("opens authoritative completion at the summary",()=>expect(initialReleaseSessionMode(true)).toBe("summary"));it("opens incomplete Lessons in normal play",()=>expect(initialReleaseSessionMode(false)).toBe("play"));it("isolates the runtime when Next Lesson changes route identity",()=>{expect(releaseLessonRuntimeKey("3","32")).not.toBe(releaseLessonRuntimeKey("3","31"));expect(initialReleaseSessionMode(true)).toBe("summary");expect(initialReleaseSessionMode(false)).toBe("play")});it("isolates the same Lesson identity across Releases",()=>expect(releaseLessonRuntimeKey("4","31")).not.toBe(releaseLessonRuntimeKey("3","31")));it("Review preserves authoritative activity completion",()=>expect(completedActivityIdsForSession("review",["1","2"])).toEqual(["1","2"]));it("Restart begins with a fresh local activity pass",()=>expect(completedActivityIdsForSession("restart",["1","2"])).toEqual([]));it("never rewrites activity progress for an already-completed Lesson",()=>expect(shouldWriteReleaseActivity(true)).toBe(false));it("still writes Release progress while first completing a Lesson",()=>expect(shouldWriteReleaseActivity(false)).toBe(true));it("repeated Restart derives a fresh pass without mutating its input",()=>{const ids=["1"];expect(completedActivityIdsForSession("restart",ids)).toEqual([]);expect(completedActivityIdsForSession("restart",ids)).toEqual([]);expect(ids).toEqual(["1"])})});
describe("Release Review action ownership",()=>{it("uses the adapter's single action group only on the final activity",()=>{expect(shouldUseFinalReviewActions(1,2,true)).toBe(true);expect(shouldUseFinalReviewActions(0,2,true)).toBe(false)});it("keeps shared Review controls when no final action group exists",()=>expect(shouldUseFinalReviewActions(0,1,false)).toBe(false))});
