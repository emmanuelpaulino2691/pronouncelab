import{describe,expect,it}from"vitest";import{mapReleaseLesson}from"./releaseLessonMapper";import type{CourseReleaseManifest,ReleaseLessonPayload}from"./releaseService";
const manifest={id:3,courseId:952001,releaseNumber:1,title:"Course",description:"",level:"A1",emoji:"",releasedAt:"2026-08-01T00:00:00Z",fingerprint:"",units:[]}satisfies CourseReleaseManifest;
const base={schemaVersion:1,courseReleaseId:3,releaseLessonId:6,lessonRevision:"952031:1",lesson:{id:"952021",unitId:"952011",title:"Pinned V1",description:"",currentVersionId:"952031",versionNumber:1,publishedAt:"2026-08-01T00:00:00Z"}}as const;
describe("Release Lesson mapping",()=>{it("maps pinned Theory blocks into the shared Lesson Player contract",()=>{const payload={...base,lesson:{...base.lesson,activities:[{id:"952041",type:"theory",title:"Learn",position:0,required:true,blocks:[{type:"paragraph",text:"Actual historical content"}]}]}}satisfies ReleaseLessonPayload;const lesson=mapReleaseLesson(payload,manifest);expect(lesson?.metadata).toMatchObject({lessonVersionId:"952031",versionNumber:1});expect(lesson?.activities[0]).toMatchObject({type:"theory",blocks:[{type:"paragraph",text:"Actual historical content"}]})});it("maps Listening media, transcript, and questions rather than a generic shell",()=>{const payload={...base,lesson:{...base.lesson,activities:[{id:"952042",type:"listening",title:"Listen",position:0,required:true,items:[{id:"1",title:"Sound",position:0,instructions:"Listen carefully",transcript:"Pinned transcript",audio:null,questions:[]}]}]}}satisfies ReleaseLessonPayload;expect(mapReleaseLesson(payload,manifest)?.activities[0]).toMatchObject({type:"listening",items:[{instructions:"Listen carefully",transcript:"Pinned transcript"}]})});it("rejects locked payloads without mapping content",()=>expect(mapReleaseLesson({schemaVersion:1,courseReleaseId:3,releaseLessonId:7,locked:true,error:{code:"release_lesson_locked",message:"Locked"},lesson:null},manifest)).toBeNull())});
