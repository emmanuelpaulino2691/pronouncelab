import { useState } from "react";import{useNavigate}from"react-router-dom";import{createClass}from"../../classes/classService";import{Button,ButtonLink,Card,FormField,PageHeader,TextArea,TextInput}from"../ui";
export function CreateClassForm(){const navigate=useNavigate();const[name,setName]=useState("");const[description,setDescription]=useState("");const[error,setError]=useState<string|null>(null);const[busy,setBusy]=useState(false);return <section className="mx-auto max-w-3xl space-y-6"><PageHeader eyebrow="My Classes" title="Create Class" breadcrumbs={[{label:"My Classes",to:"/admin/classes"},{label:"Create"}]} actions={<ButtonLink to="/admin/classes" variant="secondary">Cancel</ButtonLink>}/><Card className="p-8"><form className="space-y-5" onSubmit={e=>{e.preventDefault();setBusy(true);setError(null);void createClass(name,description).then(id=>navigate(`/admin/classes/${id}`)).catch(()=>{setError("Class could not be created.");setBusy(false);});}}><FormField label="Class name" htmlFor="class-name" required><TextInput id="class-name" value={name} maxLength={120} required onChange={e=>setName(e.target.value)}/></FormField><FormField label="Description" htmlFor="class-description" hint="Optional"><TextArea id="class-description" value={description} maxLength={1000} onChange={e=>setDescription(e.target.value)}/></FormField>{error&&<p role="alert" className="text-red-700">{error}</p>}<Button type="submit" disabled={busy||!name.trim()}>{busy?"Creating…":"Create Class"}</Button></form></Card></section>}
